name: "preprocessing"

optional_settings:
  mem_mb:
    description: "Memory requirement"
    type: "integer"
    default: "2000"

single_end:
  snakefile: "preprocessing"

paired_end:
  snakefile: "preprocessing"
